#!/bin/bash
echo "************ Cronny Started MINUTELY ***************"
minute=$(date +%M)
echo $minute;

if [ $(($minute % 5)) -eq 0 ]; then
	node ${OPENSHIFT_REPO_DIR}/cron/get-tracking.js
fi

echo "************ Cronny Executed ***************"
